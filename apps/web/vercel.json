{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "buildCommand": "bun install && cd ../server && bun run tsoa && bun run openapi",
  "outputDirectory": "apps/web/dist"
}
